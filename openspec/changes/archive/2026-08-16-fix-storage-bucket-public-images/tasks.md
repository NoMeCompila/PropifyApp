## 1. Storage RLS Policy Configuration

- [x] 1.1 Provide SQL script for `property-images` Storage bucket public read and upload RLS policies.

## 2. Frontend UI Image Resilience

- [x] 2.1 Add `onError` image fallback handler to `PropertyDetailView.tsx`.
- [x] 2.2 Add `onError` image fallback handler to `PropertyCard.tsx`.
- [x] 2.3 Add `onError` image fallback handler to `ListingsView.tsx`.

## 3. Verification & Build

- [x] 3.1 Execute `npm run build` to verify clean compilation.
- [x] 3.2 Verify image loading and fallback behavior across property catalog and detail views.
