## 1. Components & Styling

- [x] 1.1 Create `src/components/FooterBar.tsx` supporting seller 2-line layout and buyer 1-line layout.
- [x] 1.2 Implement light mode (light gray/black text) and dark mode (dark purple/white text) color styles.
- [x] 1.3 Add mobile navigation bottom padding offset (`mb-16 md:mb-0`) so content is not obscured.

## 2. Integration & Verification

- [x] 2.1 Import and render `FooterBar` in `src/App.tsx` below `<main className="flex-1">`.
- [x] 2.2 Run `npm run build` to verify clean TypeScript compilation.
- [x] 2.3 Verify rendering across all views (`catalog`, `detail`, `login`, `dashboard`) in both light and dark modes.
