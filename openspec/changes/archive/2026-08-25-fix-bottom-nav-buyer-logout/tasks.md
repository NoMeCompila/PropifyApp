## 1. BottomNavBar Component Update

- [x] 1.1 Update Buyer navigation branch in `src/components/BottomNavBar.tsx` to render `Salir` when `currentUser` is authenticated and `Ingresar` when unauthenticated

## 2. Verification

- [x] 2.1 Run TypeScript check and production build (`npm run build`) to verify clean compilation
